import * as THREE from 'three';


const vertexshader = '';

const fragmentshader = '';

export default material = new THREE.ShaderMaterial({
    vertexShader : vertexshader,
    fragmentShader : fragmentshader
});